# Legion Works Homebrew Tap

Formulas are published automatically by each [dormant](https://github.com/legion-works/dormant) release.

```sh
brew install legion-works/tap/dormantd legion-works/tap/dormantctl
```

`dormant-tray` is Linux-only today; its formula installs but the applet has no macOS surface yet.
